{
  "name": "wallet-contract",
  "version": "1.0.0",
  "description": "Hostet wallet contract built atop NEAR protocol example",
  "scripts": {
    "build": "cargo build --target wasm32-unknown-unknown --release",
    "postbuild": "cp target/wasm32-unknown-unknown/release/peleon_wallet_contract.wasm ./res/",
    "test": "cargo test --package peleon_wallet_contract -- --nocapture"
  },
  "keywords": [
    "near-protocol",
    "blockchain",
    "rust",
    "smart-contract",
    "hosted-wallet"
  ],
  "author": "Mike Purvis, Sebastien La Duca",
  "license": "MIT",
  "devDependencies": {}
}

